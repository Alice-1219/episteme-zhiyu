import { createClient } from "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm";
const C=window.EPISTEME_CONFIG||{};const sb=createClient(C.SUPABASE_URL,C.SUPABASE_PUBLISHABLE_KEY);
let lessons=[];let currentCourse=null;
function bvid(l){const s=String(l?.video_ref||l?.video_url||"");const m=s.match(/BV[0-9A-Za-z]+/i);return m?m[0]:null}
function getCourseId(){const m=location.hash.match(/^#\/course\/([^/?]+)/);return m?.[1]||null}
async function load(){const id=getCourseId();if(!id)return;const {data,error}=await sb.from("course_lessons").select("*").eq("course_id",id).order("lesson_order");if(error)return;lessons=data||[];currentCourse=id;bindLessons();renderLesson(lessons[0])}
function renderLesson(l){const host=document.querySelector(".course-player-panel .viewer > div");if(!host||!l)return;host.querySelectorAll("#player,.video-empty,.episteme-bilibili,.episteme-external").forEach(x=>x.remove());if(l.video_provider==="bilibili"){const id=bvid(l);if(!id)return;const iframe=document.createElement("iframe");iframe.className="episteme-bilibili";iframe.title=l.title||"Bilibili lesson";iframe.allow="autoplay; fullscreen; picture-in-picture";iframe.allowFullscreen=true;iframe.loading="lazy";iframe.referrerPolicy="strict-origin-when-cross-origin";iframe.style.cssText="width:100%;aspect-ratio:16/9;border:0;border-radius:16px;background:#111;display:block";iframe.src=`https://player.bilibili.com/player.html?bvid=${encodeURIComponent(id)}&poster=1&autoplay=0&danmaku=1`;host.prepend(iframe);return}if(l.video_provider==="external"&&l.video_url){const video=document.createElement("video");video.id="player";video.className="video episteme-external";video.controls=true;video.src=l.video_url;host.prepend(video);return}if(l.storage_path){const {data}=sb.storage.from("episteme-courses").getPublicUrl(l.storage_path);if(data?.publicUrl){const video=document.createElement("video");video.id="player";video.className="video";video.controls=true;video.src=data.publicUrl;host.prepend(video);return}}if(l.video_url){const video=document.createElement("video");video.id="player";video.className="video";video.controls=true;video.src=l.video_url;host.prepend(video);return}const empty=document.createElement("div");empty.className="video-empty";empty.textContent="课程视频链接尚未添加";host.prepend(empty)}
function bindLessons(){document.querySelectorAll(".course-player-panel .lesson").forEach((btn,i)=>{if(btn.dataset.mediaBound)return;btn.dataset.mediaBound="1";const original=(btn.getAttribute("onclick")||"").match(/playLesson\(['\"]([^'\"]+)/)?.[1];btn.removeAttribute("onclick");btn.onclick=e=>{e.preventDefault();document.querySelectorAll(".course-player-panel .lesson").forEach(x=>x.classList.remove("active"));btn.classList.add("active");const l=lessons.find(x=>x.id===original)||lessons[i];renderLesson(l)}})}
const observer=new MutationObserver(()=>{const id=getCourseId();if(id&&document.querySelector(".course-player-panel")){if(currentCourse!==id)load();else bindLessons()}});observer.observe(document.body,{childList:true,subtree:true});window.addEventListener("hashchange",()=>{currentCourse=null;setTimeout(load,200)});setTimeout(load,500);