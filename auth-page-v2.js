import { createClient } from "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm";
const C=window.EPISTEME_CONFIG||{};const sb=createClient(C.SUPABASE_URL,C.SUPABASE_PUBLISHABLE_KEY);const root=document.getElementById("authPage");const AV={leaf:"🍃",book:"📖",star:"✦",cloud:"☁",moon:"☾",sun:"☀",flower:"✿",mountain:"⌁",wave:"〰",cat:"◕ᴥ◕",fox:"🦊",owl:"🦉"};let mode="login";
const esc=s=>String(s||"").replace(/[&<>\"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'\"':"&quot;"}[m]));const toast=m=>{const t=document.getElementById("toast");t.textContent=m;t.classList.add("show");setTimeout(()=>t.classList.remove("show"),2200)};function err(m){const e=document.getElementById("authError");if(e){e.textContent=m;e.className="auth-error show"}}
function render(){root.innerHTML=`<div class="account-wrap"><div class="account-intro"><div class="eyebrow">EPISTEME ACCOUNT</div><h1>${mode==="login"?"Welcome back.":"Join Episteme."}</h1><p>${mode==="login"?"Sign in to continue asking questions, discussing, watching courses, and using the library.":"Create your Episteme account. After registration, choose your nickname, avatar, and language preference."}</p></div><div class="auth-layout"><section class="auth-card"><div class="auth-tabs"><button class="auth-tab ${mode==="login"?"active":""}" id="lt">Sign in</button><button class="auth-tab ${mode==="signup"?"active":""}" id="st">Sign up</button></div><form class="auth-form" id="af"><label>Email<input name="email" type="email" required autocomplete="email" placeholder="you@example.com"></label><label>Password<input name="password" type="password" required minlength="6" autocomplete="${mode==="signup"?"new-password":"current-password"}" placeholder="At least 6 characters"></label>${mode==="signup"?`<label>Confirm password<input name="confirm" type="password" required minlength="6" autocomplete="new-password" placeholder="Enter your password again"></label>`:`<div style="text-align:right"><button type="button" class="auth-link" id="forgot">Forgot username / password?</button></div>`}<div id="authError" class="auth-error"></div><button class="submit" id="go">${mode==="login"?"Sign in":"Create account"}</button><p class="auth-help">${mode==="login"?"Don't have an account? Click “Sign up” above.":"Your language preference will be chosen after registration and can be changed later in My Account."}</p></form></section><aside class="auth-side"><div class="eyebrow">A PLACE FOR KNOWLEDGE</div><h2>Your account is more than a login.</h2><p>Bring your learning identity into Episteme. Your nickname, avatar, language, and role can all be managed in My Account.</p><div class="auth-points"><div class="auth-point"><i>01</i><div><b>Nickname & avatar</b><span>Set up your profile after registration.</span></div></div><div class="auth-point"><i>02</i><div><b>Invite code</b><span>Enter an invite code to receive a role or subject access.</span></div></div><div class="auth-point"><i>03</i><div><b>Account switching</b><span>Sign out and switch to another account whenever you need.</span></div></div></div></aside></div></div>`;document.getElementById("lt").onclick=()=>{mode="login";render()};document.getElementById("st").onclick=()=>{mode="signup";render()};document.getElementById("forgot")?.addEventListener("click",forgot);document.getElementById("af").onsubmit=submit}
async function forgot(){root.innerHTML=`<div class="account-wrap"><div class="onboard"><section class="onboard-card"><div class="eyebrow">ACCOUNT RECOVERY</div><h2>Recover your account</h2><p>Enter your registration email and we will send a recovery email.</p><form class="onboard-form" id="rf"><label>Registration email<input type="email" name="email" required></label><button class="submit">Send recovery email</button><button type="button" class="secondary" id="back">Back to sign in</button></form></section></div></div>`;document.getElementById("back").onclick=render;document.getElementById("rf").onsubmit=async e=>{e.preventDefault();const email=String(new FormData(e.target).get("email")||"").trim().toLowerCase();const {error}=await sb.auth.resetPasswordForEmail(email,{redirectTo:new URL("reset-password.html",location.href).href});toast(error?"Unable to send right now. Please try again later.":"If this email belongs to an Episteme account, you will receive a recovery email.");if(!error)setTimeout(render,1200)}}
async function submit(e){e.preventDefault();const f=new FormData(e.target),email=String(f.get("email")||"").trim().toLowerCase(),password=String(f.get("password")||""),btn=document.getElementById("go");btn.disabled=true;btn.textContent="Working…";if(mode==="signup"){if(password!==String(f.get("confirm")||"")){err("The passwords do not match.");btn.disabled=false;btn.textContent="Create account";return}const {data,error}=await sb.auth.signUp({email,password,options:{data:{username:email.split("@")[0],language:"en"}}});if(error){err(error.message.includes("already registered")?"This email is already registered. Please sign in.":error.message);btn.disabled=false;btn.textContent="Create account";return}if(data.session){location.replace("./auth.html?onboarding=1");return}root.innerHTML=`<div class="account-wrap"><div class="onboard"><section class="onboard-card account"><div class="avatar">✉</div><div class="eyebrow">CHECK YOUR EMAIL</div><h2>Your account is ready</h2><p class="auth-confirm-lead">Check your email and click the <strong>Confirmation / Confirm your email</strong> link.</p><div class="auth-confirm-box"><b>Next steps</b><span>1. Open your registration email</span><span>2. Find the confirmation email from Episteme</span><span>3. Click the confirmation link</span><span>4. Return here and sign in</span></div><p class="auth-confirm-note">If you don't see it, check your <strong>Spam / Promotions</strong> folders. Delivery can take a few minutes.</p><button class="submit" id="backLogin">I have confirmed my email — sign in</button></section></div></div>`;document.getElementById("backLogin").onclick=render;return}const {error}=await sb.auth.signInWithPassword({email,password});if(error){err(error.message.includes("Invalid login")?"Incorrect password. Please try again.":error.message);btn.disabled=false;btn.textContent="Sign in";return}location.replace("./account.html")}
async function onboarding(){const {data:{session}}=await sb.auth.getSession();if(!session){render();return}const {data:pr}=await sb.from("profiles").select("*").eq("id",session.user.id).maybeSingle();let selected=pr?.avatar_key||"leaf",name=pr?.username||session.user.email?.split("@")[0]||"Member",lang="en";root.innerHTML=`<div class="account-wrap"><div class="onboard"><section class="onboard-card"><div class="stepper"><span class="step-dot active"></span><span class="step-dot active"></span><span class="step-dot active"></span></div><div class="avatar-preview" id="preview">${AV[selected]}</div><h2>One last step: make Episteme yours.</h2><p>Choose the name, avatar, and language you want to use across the entire Episteme website. You can change all of them later in My Account.</p><form class="onboard-form" id="of"><label>Nickname<input name="username" value="${esc(name)}" maxlength="30" required></label><label>Choose an avatar<div class="avatar-grid">${Object.entries(AV).map(([k,v])=>`<button type="button" class="avatar-choice ${k===selected?"selected":""}" data-a="${k}">${v}</button>`).join("")}</div></label><label>Language preference<div class="language-buttons"><button type="button" class="lang-btn selected" data-lang="en">English</button><button type="button" class="lang-btn" data-lang="zh-CN">简体中文</button></div></label><div class="role-card">Current role: <strong>${pr?.role==="coordinator"?"Coordinator":pr?.role==="subject_manager"?"Subject Manager":"Member"}</strong>.</div><div class="onboard-actions"><button class="submit">Finish setup</button></div></form></section></div></div>`;document.querySelectorAll(".avatar-choice").forEach(x=>x.onclick=()=>{selected=x.dataset.a;document.querySelectorAll(".avatar-choice").forEach(y=>y.classList.toggle("selected",y===x));document.getElementById("preview").textContent=AV[selected]});document.querySelectorAll("[data-lang]").forEach(x=>x.onclick=()=>{lang=x.dataset.lang;document.querySelectorAll("[data-lang]").forEach(y=>y.classList.toggle("selected",y===x))});document.getElementById("of").onsubmit=async e=>{e.preventDefault();const username=String(new FormData(e.target).get("username")||"").trim();const {error}=await sb.rpc("update_my_profile",{p_username:username,p_avatar_key:selected,p_language:lang});if(error){toast(error.message.includes("username")?"This nickname is already in use.":"Unable to save your profile. Please try again.");return}localStorage.setItem("episteme-language",lang);localStorage.setItem("episteme_language",lang);location.replace("./account.html")}}
(async()=>{if(location.search.includes("onboarding=1"))await onboarding();else{const {data:{session}}=await sb.auth.getSession();if(session)location.replace("./account.html");else render()}})();