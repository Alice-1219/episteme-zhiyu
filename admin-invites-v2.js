import { createClient } from "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm";
const C=window.EPISTEME_CONFIG||{};const sb=createClient(C.SUPABASE_URL,C.SUPABASE_PUBLISHABLE_KEY);const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));let subjects=[],busy=false;
async function loadSubjects(){const {data,error}=await sb.from('subjects').select('id,name,code').order('name');if(!error)subjects=data||[]}
async function loadInvites(){const {data:invites,error}=await sb.from('manager_invites').select('id,active,created_at,label,role').eq('active',true).order('created_at',{ascending:false});if(error)throw error;const ids=(invites||[]).map(x=>x.id);if(!ids.length)return[];const {data:links,error:linkError}=await sb.from('manager_invite_subjects').select('invite_id,subject_id,subjects(id,name,code)').in('invite_id',ids);if(linkError)throw linkError;return(invites||[]).map(i=>({...i,subjects:(links||[]).filter(x=>x.invite_id===i.id).map(x=>x.subjects).filter(Boolean)}))}
function subjectPicker(){return`<div class="invite-subject-picker">${subjects.map(s=>`<label class="invite-subject-option"><input type="checkbox" name="subject_ids" value="${s.id}"><span><b>${esc(s.name)}</b>${s.code?`<small>${esc(s.code)}</small>`:''}</span></label>`).join('')}</div>`}
function cardShell(){return[...document.querySelectorAll('.admin-card')].find(x=>x.querySelector('h2')?.textContent.includes('生成管理人邀请码'))}
function styles(){if(document.getElementById('inviteV2Styles'))return;const style=document.createElement('style');style.id='inviteV2Styles';style.textContent=`.invite-subject-picker{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:4px}.invite-subject-option{display:flex!important;grid-template-columns:none!important;flex-direction:row;align-items:center;gap:10px;padding:11px 12px;border:1px solid #dfe4dd;border-radius:12px;background:#fff;cursor:pointer}.invite-subject-option:has(input:checked){background:#edf4e8;border-color:#9eb49a}.invite-subject-option input{width:auto!important;margin:0}.invite-subject-option span{display:grid;gap:2px}.invite-subject-option small{color:#7a837d}.invite-v2-list{display:grid;gap:10px}.invite-v2-row{display:flex;justify-content:space-between;align-items:center;gap:14px;padding:14px;border:1px solid #e4e8e2;border-radius:14px}.invite-v2-row .scope{display:block;color:#737c76;margin-top:4px;line-height:1.5}@media(max-width:800px){.invite-subject-picker{grid-template-columns:1fr}.invite-v2-row{align-items:flex-start}}`;document.head.appendChild(style)}
async function renderInviteCard(card){if(!card||card.dataset.inviteV2==='1')return;card.dataset.inviteV2='1';styles();await loadSubjects();let invites=[];try{invites=await loadInvites()}catch(e){console.warn('invite list load failed',e)}card.innerHTML=`<h2>生成管理人邀请码</h2><div class="section-note">一个邀请码可以绑定多个学科。先选择负责学科，再填写岗位 / 管理人名称；对方输入一次邀请码后，邀请码会立即失效。</div><form id="inviteV2Form" class="form-grid"><label>负责学科（可多选）${subjectPicker()}</label><label>岗位 / 管理人名称<input name="label" maxlength="80" placeholder="例如：张三 — 理科负责人" required></label><button class="primary" type="submit">生成邀请码</button></form><div style="height:22px"></div><h3 style="margin:0 0 12px">已开通的岗位</h3><div class="invite-v2-list" id="inviteV2List">${invites.length?invites.map(i=>`<div class="invite-v2-row"><div><b>${esc(i.label||'未命名管理岗位')}</b><span class="scope">${i.subjects.length?i.subjects.map(s=>esc(s.name+(s.code?' · '+s.code:''))).join(' / '):'未绑定学科'}</span></div><span class="role-pill">已开通</span></div>`).join(''):'<div class="empty-admin">暂无已开通岗位</div>'}</div>`;document.getElementById('inviteV2Form')?.addEventListener('submit',createInvite)}
async function createInvite(e){e.preventDefault();if(busy)return;busy=true;const f=new FormData(e.currentTarget),subjectIds=[...f.getAll('subject_ids')].map(Number).filter(Boolean),label=String(f.get('label')||'').trim();if(!subjectIds.length){alert('请至少选择一个负责学科。');busy=false;return}if(!label){alert('请填写岗位 / 管理人名称。');busy=false;return}const alphabet='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';for(let attempt=0;attempt<5;attempt++){const bytes=new Uint32Array(8);crypto.getRandomValues(bytes);const code=[...bytes].map(x=>alphabet[x%alphabet.length]).join('');const {data:existing,error:checkError}=await sb.from('manager_invites').select('id').eq('code',code).maybeSingle();if(checkError){alert(checkError.message);busy=false;return}if(existing)continue;const {data:invite,error}=await sb.from('manager_invites').insert({code,label,role:'subject_manager',active:true,subject_id:null}).select('id').single();if(error){alert(error.message);busy=false;return}const rows=subjectIds.map(subject_id=>({invite_id:invite.id,subject_id}));const {error:linkError}=await sb.from('manager_invite_subjects').insert(rows);if(linkError){await sb.from('manager_invites').update({active:false}).eq('id',invite.id);alert(linkError.message);busy=false;return}alert(`邀请码已生成：${code}\n\n${label}\n负责：${subjectIds.map(id=>subjects.find(s=>Number(s.id)===id)?.name||id).join(' / ')}\n\n请将邀请码交给对应管理人；对方输入一次后立即失效。`);const card=cardShell();if(card){card.removeAttribute('data-invite-v2');await renderInviteCard(card)}busy=false;return}alert('邀请码生成失败，请重试。');busy=false}
let scheduled=false;function enhance(){if(scheduled)return;scheduled=true;setTimeout(async()=>{scheduled=false;const card=cardShell();if(card&&card.dataset.inviteV2!=='1')await renderInviteCard(card)},80)}const observer=new MutationObserver(enhance);observer.observe(document.body,{childList:true,subtree:true});setTimeout(enhance,500);setInterval(enhance,1500);