# Episteme V3 Homepage

Standalone new public homepage.

Replace the old public homepage with `index.html` and `styles.css`.

This package intentionally does not contain Supabase/auth/course/resource/backend code yet. The two main buttons are visual placeholders for the V3 routes.

The homepage uses the new Episteme copy and a restrained university/academic visual style.
